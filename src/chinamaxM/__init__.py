"""chinamaxM — worker-model native-subagent plugin, proxy service package.

This package hosts the local reverse Proxy and its Registry. See ``CONTEXT.md`` for
the domain vocabulary (Proxy, Ingress, Profile, Profile prefix, Default branch).
"""
