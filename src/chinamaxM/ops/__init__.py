"""chinamaxM operations subpackage: OS-native Proxy supervision.

See :mod:`chinamaxM.ops.supervision` for the systemd user / launchd / WinSW machinery
that Setup drives (install/update/teardown) and Doctor inspects (status primitives),
per ADR 0009 as amended.
"""
