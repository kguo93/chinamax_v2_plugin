# chinamax_v2_plugin — inventory

ADR-round repo for **chinamaxM**, the worker-model native-subagent plugin (successor to
`~/chinamax_plugin`). The proxy service and Host surfaces are under implementation
(proxy-01/02/03/04, hosts-01 generators, hosts-02 Claude dispatch + worker-contract hooks,
hosts-03 Codex dispatch skill + Codex hook adapter, ops-01 systemd/launchd/WinSW
supervision, ops-02 GitHub-canonical dual manifests + packaging, hosts-04 diagnosis
surfaces — `/doctor` roster + `/profiles` + warn-only
SessionStart hook, and hosts-05 `/setup` — the one consent-gated mutating surface —
landed): the `docs/adr/` decisions
of record plus the `src/`/`tests/` code and the plugin
`commands/`/`skills/`/`hooks/`/`scripts/`/`.codex-plugin/` surfaces that realize them.

| Path | What it is |
|---|---|
| `CLAUDE.md` | root conventions (secrets rule) |
| `AGENTS.md` | stub pointing Codex/other agents at `CLAUDE.md` |
| `CONTEXT.md` | the domain glossary — Proxy, Ingress, Profile, Worker, Relay, Seam, … |
| `repo-map.md` | this inventory |
| `README.md` | user-facing guide: intro + requirements (platform prerequisites — bash / Git for Windows / Miniconda — detected and installed for you on approval, no pre-installed Python needed via the Windows zero-state, manual only on unsupported CPU arch), both-Hosts command/skill map, install/upgrade (both Hosts' `marketplace add`/install/add, GitHub-only/rpi4-git-backup rule), the `/setup` plan→approve→apply flow, per-Host key files, per-OS proxy supervision table (systemd+linger / launchd / WinSW), `/task` dispatch + steer/continue + live-session-only resume, `/doctor`+`/profiles`, teardown |
| `.gitignore` | build/scratch byproduct patterns (`.scratch/`, `__pycache__/`, `*.pyc`, `*.egg-info/`, `.pytest_cache/`, `build/`, `dist/`, vim swaps) + `docs/plan/` (ephemeral local plan-of-record scratch) |
| `pyproject.toml` | package metadata (name `chinamaxM`, version, shared `description`, `license = "GPL-2.0-only"` per PEP 639, author) + runtime deps (aiohttp, litellm pin, tomlkit, PyYAML) + `test` extra (pytest, pytest-aiohttp) |
| `docs/` | sketch seed + conventions + `docs/adr/` decisions of record (see `docs/repo-map.md`) |
| `src/chinamaxM/` | the proxy package: `registry.py` (Registry loader), `proxy.py` (aiohttp app + router + Default/Relay/Responses ingresses + per-request JSONL logging + count_tokens forward/estimator), `relay.py` (relay mutation/serialize/forwarder, optional usage tee), `keyfiles.py` (per-Host Key-file reader + O_EXCL scaffold; the shared Host-resolution ladder `resolve_host`/`HostResolutionError` — explicit `--host` → `CHINAMAXM_HOST` marker → Codex plugin evidence → Claude evidence → hard error, every mutating/diagnosis surface resolves its single invoking Host through it), `seam.py` (LiteLLM Seam: Responses⇄Anthropic translation + streaming state machine), `observability.py` (append-only JSONL request log, provider-usage tee, count_tokens estimator, log-path resolver), `doctor.py` (read-only diagnosis engine, Host-scoped via `--host`/the shared ladder: `Finding` roster with per-check isolation + injectable runners, each check tagged shared/claude/codex and filtered so only the invoking Host's wiring appears, drift+keys checks branch on `ctx.host`, FAIL/WARN/info checks reusing registry/keyfiles/generate/supervision/observability, host-scoped `render_profiles` single key column, the shared `normalize_flip_url`, and the static no-chat-completions scan `find_chat_completions_references`), `generate.py` (Registry→Claude agents + Codex providers/roles generation engine, Host-selectable via a `host` selector on `expected_artifacts`/`detect_drift`/`regenerate` — Claude writes only the `.md`s, Codex only the provider entries + role TOMLs; strict whole-content drift detection, marker-safe regeneration, `remove_provider_entries` teardown unwire; artifacts are immutable outside generation — a per-dispatch model override rides the Dispatch marker `[chinamaxm model=…]` resolved by `relay.resolve_dispatch_marker` inside `mutate_body`, ADR 0004 as amended 2026-08-19), `hooks/worker_contract.py` (SubagentStart Worker-contract + PreToolUse(Agent) verbatim-Relay injector; `matches_generated_agent` anchored matcher lives in `generate.py` and accepts the bare Profile plus BOTH per-Host instance grammars — `chinamaxm-<profile>-<suffix>` Claude, `chinamaxm_<profile>_<suffix>` Codex), `hooks/session_start.py` (host-aware warn-only SessionStart hook: resolves the Host through the shared `keyfiles.resolve_host` ladder (Codex evidence outranks Claude aliases), and — unlike the mutating surfaces — treats an unresolvable Host as a SILENT exit 0 (fail-open, never guesses claude); Claude warns iff the `ANTHROPIC_BASE_URL` flip is present but its loopback port is dead (→ `/chinamaxM:doctor`), Codex warns iff a generated `model_providers.chinamaxM-*` entry is in `~/.codex/config.toml` AND the Registry Proxy port is dead (→ `chinamaxM-doctor` skill); fail-open, never blocks), `ops/supervision.py` (OS-native Proxy supervision — systemd user / launchd / WinSW template rendering, converge install/update/teardown, and installed/enabled/running/port-live status primitives through an injectable runner; standalone `port_live` + `loginctl` linger helpers; `ensure_winsw_exe` Windows WinSW resolver — operator override → already-installed → pinned SHA-256-verified download, fail-closed, injectable downloader/hasher), `settings_json.py` (fail-closed atomic read-modify-write editor for the `env.ANTHROPIC_BASE_URL` flip in Claude `settings.json` — the only key it owns), `setup.py` (the one consent-gated mutating surface, Host-scoped via `--host`/the shared ladder — a Claude run wires only Claude (Key file + agents + `ANTHROPIC_BASE_URL` flip), a Codex run only Codex (Key file + provider/role generation + strict-config validate, no flip), the shared conda/proxy/service/linger infra converges either way, probes are NOT Host-scoped (both ingresses per Profile), and teardown always removes the shared service + recorded interpreter plus the invoking Host's unwire (Claude flip-remove / Codex `codex-unwire` stripping our `model_providers.chinamaxM-*` via `generate.remove_provider_entries`) with a report NOTE when the other Host stays wired: `SetupEngine` running diagnose→plan→approve→apply→re-diagnose→report + teardown, a `_Runner` that hard-errors on any pre-approval mutating op, digest-bound approval over the canonical structured plan, Platform-Prerequisite detection + agent-run Rectification rows (`_prerequisite_status`/`_prerequisite_fixes` + per-OS row builders emit `bash`/Git-for-Windows/`miniconda` rows the HOST runs on approval — the engine NEVER downloads or runs an installer; `--plan-only` PAUSES with `prerequisite_fixes` and NO digest when any is missing (Phase A); `~/miniconda3` probe via `_SubprocessConda._conda_bin`, Miniconda `Miniconda3-latest-*` NO pin/checksum, unsupported-arch advice row, Windows zero-state cmd.exe fallback; Windows mocked-only), opt-in dual-ingress live probes, Windows WinSW auto-acquire via `ensure_winsw_exe` with `--winsw-exe`/`--winsw-service-password-file` CLI overrides (WinSW source shown in the plan + bound in the digest), records the hook-shim rung-2 interpreter at `<claude-root>/chinamaxM/python-path` (apply step; teardown removes it), all roots/commands/HTTP injectable; composes doctor/generate/keyfiles/supervision/settings_json), `data/profiles.json` (shipped seed Registry) |
| `commands/` | plugin slash-commands: `task.md` (`/chinamaxM:task` Claude dispatch protocol — profile-required, model never validated, named background spawn, verbatim Relay first copy; an explicit `model=` prepends the Dispatch-marker line `[chinamaxm model=<model>]` to the spawn prompt — no file rewrite exists); `doctor.md` (`/chinamaxM:doctor` — thin wrapper running the Launcher `scripts/chinamaxM doctor` + degraded-launch fallback pointing at `/chinamaxM:setup`); `profiles.md` (`/chinamaxM:profiles` — same Launcher with `doctor --profiles`); `setup.md` (`/chinamaxM:setup` — the consent-gated mutating surface: launch via the Launcher `scripts/chinamaxM setup --plan-only` (`allowed-tools` narrowed to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/chinamaxM:*)`), which resolves its own interpreter with no ambient `PYTHONPATH`; a prerequisite pause (missing bash/Git-for-Windows/Miniconda, no digest) triggers the "approve"→run-rows-by-`run_policy`-through-each-row's-`shell`, stop-on-first-failure→re-run-once protocol + the Windows zero-state cmd.exe block + the macOS CLT-stub operator kick-back; the normal plan gets two distinct consents (apply + optional probes) then `--apply --plan-digest <d>`; teardown + concurrency + no-key-value notes) |
| `skills/` | Codex plugin skills: `chinamaxM-task/SKILL.md` (Codex dispatch twin — same `profile=`/`model=`/`name=` grammar, delegation-authorization sentence, `spawn_agent` of the bare role, `< /dev/null` headless close, parent-mediated steer via `send_input`/`followup_task`, ADR 0008 resume gotchas, verbatim Relay first copy; an explicit `model=` prepends the Dispatch-marker line to the spawn message, and instance names use the Codex underscore grammar `chinamaxm_<profile>_<suffix>`); `chinamaxM-doctor/SKILL.md` + `chinamaxM-profiles/SKILL.md` (Codex twins of the doctor/profiles commands — same Launcher `scripts/chinamaxM doctor [--profiles]`, `< /dev/null` headless close, degraded-launch fallback); `chinamaxM-setup/SKILL.md` (Codex twin of `/chinamaxM:setup` — same prerequisite-pause "approve"→run-rows→re-run-once protocol + Windows zero-state block + macOS CLT-stub operator kick-back, then the `--plan-only`/`--apply --plan-digest`/`--teardown` flow launched via the Launcher `scripts/chinamaxM setup …` (self-resolving interpreter, no ambient `PYTHONPATH`), `< /dev/null` headless close, two distinct consents). The Codex Host now DOES carry a warn-only SessionStart hook (see `hooks/codex-hooks.json`), symmetric with Claude's |
| `hooks/` | `hooks.json` (Claude: SubagentStart no-matcher + PreToolUse(Agent) → `scripts/worker_contract_hook`, plus hosts-04's additive SessionStart(`startup\|resume\|clear\|compact\|fork`) → `scripts/session_start_hook`) and `codex-hooks.json` (Codex: SubagentStart no-matcher + PreToolUse(spawn_agent) → `scripts/codex_worker_contract_hook`, plus the additive SessionStart(`startup\|resume\|clear\|compact\|fork`) → `scripts/session_start_hook` (Claude-symmetric — the same host-aware module), all with `commandWindows` Git-Bash shims); all additive, `timeout: 10` |
| `scripts/` | `chinamaxM` (the CLI Launcher every host surface runs plugin Python through — maps `{setup\|doctor}` to its module and `chinamaxm_exec`s it under the resolved interpreter) sourcing `_interpreter.sh` (THE shared interpreter-discovery library — sourced, NOT executable, no shebang — with the pinned rungs: recorded `<claude-root>/chinamaxM/python-path` → `$CHINAMAXM_PYTHON` → `~/miniconda3/envs/chinamaxM` python → validated `conda run -n chinamaxM` → base `~/miniconda3` python + `src/` on `PYTHONPATH` → ambient `python3`/`python` + `src/`, plus the macOS CLT-stub operator kick-back); `worker_contract_hook` (Claude shim) and `codex_worker_contract_hook` (Codex adapter shim: translates `spawn_agent`→`Agent` + `tool_input.agent_type`→`subagent_type`, then pipes to the shared module); `session_start_hook` (SessionStart shim for BOTH Hosts → the host-aware `chinamaxM.hooks.session_start`; Codex reaches it via `codex_hook_bash.cmd session_start_hook`) — the three hook shims all SOURCE `_interpreter.sh` (rungs 1–3 + fail-open `conda run --no-capture-output`), recorded-path-first, never the launcher's bootstrap/ambient rungs (5–6); `codex_hook_bash.cmd` (Windows Git-Bash wrapper for the Codex commandWindows shims, no interpreter-discovery logic); the `CLAUDE.md`/`repo-map.md`/`AGENTS.md` conventions trio |
| `.claude-plugin/` | Claude Host manifests: `plugin.json` (plugin `chinamaxm`, version synced with the other three version-bearing locations, shared description, author Kevin Guo) + `marketplace.json` (Anthropic `$schema`, marketplace `chinamaxm-plugin`, top-level shared description, single entry `source: "./"` + version) — ADR 0012 pinned identities |
| `.codex-plugin/` | `plugin.json` — Codex plugin manifest: full metadata (`chinamaxm`, synced version, GitHub homepage/repository, license `GPL-2.0` literal, `interface` block: `ChinamaXM` displayName + capabilities) plus `skills` → `./skills` and the REQUIRED `hooks` → `./hooks/codex-hooks.json` (real Codex auto-discovers only the fixed default `hooks/hooks.json`, so the key is needed to load the Codex-flavored hook file; the bundled plugin-creator validator false-positive-rejects the key but is not a gate — ADR 0012 amended 2026-08-14) |
| `.agents/plugins/` | `marketplace.json` — Codex catalog: top-level `chinamaxm-plugin` + `interface.displayName chinamaxM`, single entry `source {local, ./}`, `policy {AVAILABLE, ON_INSTALL}`, category Developer tools (no version/author — Codex catalog shape) |
| `tests/` | hermetic pytest suite: `conftest.py` (fake provider + fake Codex client + guards + fixtures), `test_registry.py`, `test_proxy.py` (passthrough + routing + relay), `test_seam.py` (Responses ingress + Seam translation + streaming + startup gates), `test_observability_count_tokens.py` (JSONL line fields + usage tee + count_tokens forward/estimator/cache), `test_generate.py` (generation engine: Claude agents/Codex providers/roles + strict drift classification + the dual-grammar `matches_generated_agent` matcher, against temp roots), `test_hosts_claude.py` (`/task` command-text invariants + hooks.json registration + LIVE worker-contract/Relay hook invocation), `test_hosts_codex.py` (Codex skill-text invariants + LIVE codex-hooks shim invocation w/ payload translation + Claude↔Codex surface symmetry + `.codex-plugin` registration + Codex SessionStart registration/Git-Bash-`commandWindows`/Claude-symmetry + LIVE host-aware Codex SessionStart across configured/dead, unconfigured, no-config, and live-port states), `test_supervision.py` (per-OS unit rendering + space/% escaping, mocked-runner converge ops with exact-command assertions incl. WinSW stop/uninstall-before-replace + `sc.exe config … password=`, distinct status primitives with a real closed-port socket, config validation + linger helpers), `test_doctor.py` (doctor roster: every FAIL/WARN check fired against a valid fixture install with injected runners, exit semantics, no-mutation/no-spend, `/profiles` renderer + key-value canary, LIVE SessionStart shim across all five states, registry-failure skip cascade, Codex surface twins), `test_plugin_manifest.py` (distribution manifests: pinned identities + version agreement across the four version-bearing locations + Codex-catalog exact key sets + `.gitignore` coverage + README source lines + entropy-shaped secret sweep over `git ls-files`), `test_setup.py` (setup engine against temp roots with fake conda/service/HTTP/diagnose seams: no-mutation-before-approval via the `_Runner` label guard, per-step apply incl. env-block/generation/service install-vs-update/linger, probe opt-in separation + minimal per-ingress requests, teardown exactness + best-effort independence, re-diagnose in report, no-key-value canary, apply-failure-aborts-rest, digest/precondition/ownership gates, `--plan-only` read-only CLI, `settings_json` fail-closed, per-OS Platform-Prerequisite row emission (exact command/flag/shell/run_policy strings for bash/Git-for-Windows/miniconda incl. winget present-vs-absent + unsupported-arch advice) + the Phase-A plan-only pause (`prerequisite_fixes`, no digest) + the setup-surface protocol invariants across `commands/setup.md` and `chinamaxM-setup/SKILL.md`, now asserting the shim launch line + macOS kick-back section; plus the Launcher subprocess tests — rung precedence (recorded → `$CHINAMAXM_PYTHON` → env python → base-miniconda `PYTHONPATH`, stale-recorded fall-through), macOS CLT-stub guard, Windows `python.exe`/base/ambient branch, unknown-subcommand usage, and the no-hardcoded-interpreter docs regression guard over every `commands/*.md`/`skills/*/SKILL.md`). The host suites add the hook recorded-path-precedes-`$CHINAMAXM_PYTHON` precedence test and migrate their launcher-line text invariants to the shim form |
