# chinamax_v2_plugin — inventory

ADR-round repo for **chinamaxM**, the worker-model native-subagent plugin (successor to
`~/chinamax_plugin`). The proxy service and Host surfaces are under implementation
(proxy-01/02/03/04, hosts-01 generators, hosts-02 Claude dispatch + worker-contract hooks,
hosts-03 Codex dispatch skill + Codex hook adapter, ops-01 systemd/launchd/WinSW
supervision, ops-02 GitHub-canonical dual manifests + packaging, hosts-04 diagnosis
surfaces — `/doctor` roster + `/profiles` + warn-only
SessionStart hook, and hosts-05 `/setup` — the one consent-gated mutating surface —
landed): the `docs/adr/` decisions
of record plus the `src/`/`tests/` code and the plugin
`commands/`/`skills/`/`hooks/`/`scripts/`/`.codex-plugin/` surfaces that realize them.

| Path | What it is |
|---|---|
| `CLAUDE.md` | root conventions (secrets rule) |
| `AGENTS.md` | stub pointing Codex/other agents at `CLAUDE.md` |
| `CONTEXT.md` | the domain glossary — Proxy, Ingress, Profile, Worker, Relay, Seam, … |
| `repo-map.md` | this inventory |
| `README.md` | one-line intro + GitHub install/upgrade section (both Hosts' `marketplace add`/install/add, upgrade lines, rpi4-git-only rule, empty-value key examples) |
| `.gitignore` | build/scratch byproduct patterns (`.scratch/`, `__pycache__/`, `*.pyc`, `*.egg-info/`, `.pytest_cache/`, `build/`, `dist/`, vim swaps) |
| `pyproject.toml` | package metadata (name `chinamaxM`, version, shared `description`, `license = "GPL-2.0-only"` per PEP 639, author) + runtime deps (aiohttp, litellm pin, tomlkit, PyYAML) + `test` extra (pytest, pytest-aiohttp) |
| `docs/` | sketch seed + conventions + `docs/adr/` decisions of record (see `docs/repo-map.md`) |
| `src/chinamaxM/` | the proxy package: `registry.py` (Registry loader), `proxy.py` (aiohttp app + router + Default/Relay/Responses ingresses + per-request JSONL logging + count_tokens forward/estimator), `relay.py` (relay mutation/serialize/forwarder, optional usage tee), `keyfiles.py` (per-Host Key-file reader + O_EXCL scaffold), `seam.py` (LiteLLM Seam: Responses⇄Anthropic translation + streaming state machine), `observability.py` (append-only JSONL request log, provider-usage tee, count_tokens estimator, log-path resolver), `doctor.py` (read-only diagnosis engine: `Finding` roster with per-check isolation + injectable runners, FAIL/WARN/info checks reusing registry/keyfiles/generate/supervision/observability, `render_profiles`, the shared `normalize_flip_url`, and the static no-chat-completions scan `find_chat_completions_references`), `generate.py` (Registry→Claude agents + Codex providers/roles generation engine, drift detection, marker-safe regeneration, `set_model`), `set_model.py` (`python -m` model-line-rewrite dispatch conduit), `hooks/worker_contract.py` (SubagentStart Worker-contract + PreToolUse(Agent) verbatim-Relay injector; `matches_generated_agent` anchored matcher lives in `generate.py`), `hooks/session_start.py` (warn-only SessionStart hook: emits a `systemMessage` pointing at `/chinamaxM:doctor` iff the env flip is present but its loopback port is dead; fail-open, never blocks), `ops/supervision.py` (OS-native Proxy supervision — systemd user / launchd / WinSW template rendering, converge install/update/teardown, and installed/enabled/running/port-live status primitives through an injectable runner; standalone `port_live` + `loginctl` linger helpers), `settings_json.py` (fail-closed atomic read-modify-write editor for the `env.ANTHROPIC_BASE_URL` flip in Claude `settings.json` — the only key it owns), `setup.py` (the one consent-gated mutating surface: `SetupEngine` running diagnose→plan→approve→apply→re-diagnose→report + teardown, a `_Runner` that hard-errors on any pre-approval mutating op, digest-bound approval over the canonical structured plan, opt-in dual-ingress live probes, all roots/commands/HTTP injectable; composes doctor/generate/keyfiles/supervision/settings_json), `data/profiles.json` (shipped seed Registry) |
| `commands/` | plugin slash-commands: `task.md` (`/chinamaxM:task` Claude dispatch protocol — profile-required, model never validated, named background spawn, verbatim Relay first copy); `doctor.md` (`/chinamaxM:doctor` — thin wrapper running `conda run -n chinamaxM python -m chinamaxM.doctor` + degraded-launch fallback pointing at `/chinamaxM:setup`); `profiles.md` (`/chinamaxM:profiles` — same CLI with `--profiles`); `setup.md` (`/chinamaxM:setup` — the consent-gated mutating surface: launch under the ambient Python with `${CLAUDE_PLUGIN_ROOT}/src` on `PYTHONPATH`, run `--plan-only`, get two distinct consents (apply + optional probes), then `--apply --plan-digest <d>`; teardown + concurrency + no-key-value notes) |
| `skills/` | Codex plugin skills: `chinamaxM-task/SKILL.md` (Codex dispatch twin — same `profile=`/`model=`/`name=` grammar, delegation-authorization sentence, `spawn_agent` of the bare role, `< /dev/null` headless close, parent-mediated steer via `send_input`/`followup_task`, ADR 0008 resume gotchas, verbatim Relay first copy); `chinamaxM-doctor/SKILL.md` + `chinamaxM-profiles/SKILL.md` (Codex twins of the doctor/profiles commands — same pinned CLI, `< /dev/null` headless close, degraded-launch fallback; no Codex SessionStart twin); `chinamaxM-setup/SKILL.md` (Codex twin of `/chinamaxM:setup` — same `--plan-only`/`--apply --plan-digest`/`--teardown` flow, ambient-Python launch with the plugin `src/` on `PYTHONPATH`, `< /dev/null` headless close, two distinct consents) |
| `hooks/` | `hooks.json` (Claude: SubagentStart no-matcher + PreToolUse(Agent) → `scripts/worker_contract_hook`, plus hosts-04's additive SessionStart(`startup\|resume\|clear\|compact\|fork`) → `scripts/session_start_hook`) and `codex-hooks.json` (Codex: SubagentStart no-matcher + PreToolUse(spawn_agent) → `scripts/codex_worker_contract_hook`, with `commandWindows` Git-Bash shims); all additive, `timeout: 10` |
| `scripts/` | `worker_contract_hook` (Claude shim) and `codex_worker_contract_hook` (Codex adapter shim: translates `spawn_agent`→`Agent` + `tool_input.agent_type`→`subagent_type`, then pipes to the shared module); `session_start_hook` (Claude SessionStart shim → `chinamaxM.hooks.session_start`) — all executable, same pinned interpreter order (`CHINAMAXM_PYTHON` → setup-recorded path → `conda run` last resort); `codex_hook_bash.cmd` (Windows Git-Bash wrapper for the Codex commandWindows shims) |
| `.claude-plugin/` | Claude Host manifests: `plugin.json` (plugin `chinamaxm` 0.1.0, shared description, author Kevin Guo) + `marketplace.json` (Anthropic `$schema`, marketplace `chinamaxm-plugin`, single entry `source: "./"` + version) — ADR 0012 pinned identities |
| `.codex-plugin/` | `plugin.json` — Codex plugin manifest: full metadata (`chinamaxm` 0.1.0, GitHub homepage/repository, license `GPL-2.0` literal, `interface` block: `ChinamaXM` displayName + capabilities) plus `skills` → `./skills`; NO top-level `hooks` key (the plugin-creator validator rejects it — `./hooks/codex-hooks.json` is default-discovered, so hosts-03's Codex hooks stay registered) |
| `.agents/plugins/` | `marketplace.json` — Codex catalog: top-level `chinamaxm-plugin` + `interface.displayName chinamaxM`, single entry `source {local, ./}`, `policy {AVAILABLE, ON_INSTALL}`, category Developer tools (no version/author — Codex catalog shape) |
| `tests/` | hermetic pytest suite: `conftest.py` (fake provider + fake Codex client + guards + fixtures), `test_registry.py`, `test_proxy.py` (passthrough + routing + relay), `test_seam.py` (Responses ingress + Seam translation + streaming + startup gates), `test_observability_count_tokens.py` (JSONL line fields + usage tee + count_tokens forward/estimator/cache), `test_generate.py` (generation engine: Claude agents/Codex providers/roles + drift classification + set_model, against temp roots), `test_hosts_claude.py` (`/task` command-text invariants + hooks.json registration + LIVE worker-contract/Relay hook invocation), `test_hosts_codex.py` (Codex skill-text invariants + LIVE codex-hooks shim invocation w/ payload translation + Claude↔Codex surface symmetry + `.codex-plugin` registration), `test_supervision.py` (per-OS unit rendering + space/% escaping, mocked-runner converge ops with exact-command assertions incl. WinSW stop/uninstall-before-replace + `sc.exe config … password=`, distinct status primitives with a real closed-port socket, config validation + linger helpers), `test_doctor.py` (doctor roster: every FAIL/WARN check fired against a valid fixture install with injected runners, exit semantics, no-mutation/no-spend, `/profiles` renderer + key-value canary, LIVE SessionStart shim across all five states, registry-failure skip cascade, Codex surface twins), `test_plugin_manifest.py` (distribution manifests: pinned identities + version agreement across the four version-bearing locations + Codex-catalog exact key sets + `.gitignore` coverage + README source lines + entropy-shaped secret sweep over `git ls-files`), `test_setup.py` (setup engine against temp roots with fake conda/service/HTTP/diagnose seams: no-mutation-before-approval via the `_Runner` label guard, per-step apply incl. env-block/generation/service install-vs-update/linger, probe opt-in separation + minimal per-ingress requests, teardown exactness + best-effort independence, re-diagnose in report, no-key-value canary, apply-failure-aborts-rest, digest/precondition/ownership gates, `--plan-only` read-only CLI, `settings_json` fail-closed) |
